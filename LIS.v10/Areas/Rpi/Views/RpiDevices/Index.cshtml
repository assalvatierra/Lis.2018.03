@model IEnumerable<LIS.v10.Areas.Rpi.Models.RpiDevice>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Description)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.RpiVersion.VersionNo)
        </th>
        <th>
            Data Logs
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Description)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.RpiVersion.VersionNo)
        </td>
        <td >
            @Html.ActionLink("View Logs", "LogDetails", "RpiDatalogs",new { id = item.Id },null) 
        </td>
        <td>
            @Html.ActionLink("Settings", "Index", "RpiControls", new { id = item.Id }, null)
        </td>

        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>

<p>
    @Html.ActionLink("Home", "Index", "Home", new { area = "" }, null)
</p>